<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Keystore keys are removed on uninstall, so these blobs cannot be restored. -->
    <exclude domain="sharedpref" path="keyring-default.xml" />
    <exclude domain="root" path="session.enc" />
</full-backup-content>
